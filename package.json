{
  "name": "mekong-explorer-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "api",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1005.0",
    "@nestjs/jwt": "^11.0.2",
    "@nestjs/passport": "^11.0.5",
    "@types/bcrypt": "^6.0.0",
    "@types/multer": "^2.1.0",
    "@types/passport-jwt": "^4.0.1",
    "@types/uuid": "^10.0.0",
    "bcrypt": "^6.0.0",
    "mapbox-gl": "^3.20.0",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "uuid": "^13.0.0"
  }
}
